<!DOCTYPE html>
<html>
<head>
    <title>TaskConnect Platform</title>
    <style>
        body { font-family: Arial; max-width: 800px; margin: 0 auto; padding: 20px; }
        .header { background: #3b82f6; color: white; padding: 20px; border-radius: 10px; }
        .calculator { background: #f3f4f6; padding: 20px; border-radius: 10px; margin: 20px 0; }
        button { background: #10b981; color: white; border: none; padding: 10px 20px; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>🚀 TaskConnect Platform</h1>
        <p>Connect Clients with Workers | 20% Platform Commission</p>
    </div>
    
    <div class="calculator">
        <h2>💰 Commission Calculator</h2>
        <input type="number" id="amount" value="100" placeholder="Task amount ($)">
        <p>Commission: <span id="rate">20%</span></p>
        <button onclick="calculate()">Calculate</button>
        <div id="result"></div>
    </div>
    
    <script>
        function calculate() {
            const amount = document.getElementById('amount').value;
            const commission = amount * 0.20;
            const worker = amount - commission;
            
            document.getElementById('result').innerHTML = `
                <p>Total: $${amount}</p>
                <p>Platform (20%): $${commission}</p>
                <p><strong>Worker gets: $${worker}</strong></p>
            `;
        }
    </script>
</body>
</html>